// Change this value once to point the whole frontend at a different backend.
// Set VITE_API_URL in a .env file to override it per environment.
export const API_URL =
  import.meta.env.VITE_API_URL || "https://ai-invoice-reader-backend.onrender.com";